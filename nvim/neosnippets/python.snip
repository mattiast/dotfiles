snippet     logging
abbr        log = logging.getLogger(..)
options     head
    import logging
    import sys

    log = logging.getLogger(__name__)
    logging.basicConfig(stream=sys.stdout, level=logging.INFO)

snippet     writejson
abbr        json.dump
	import json
    with open('$1.json', 'w') as f:
        json.dump(${1:stuff}, f)
